---
description: Analyze test coverage for feature branch
---

Load the `test-coverage-analysis` skill. If an argument is provided, pass it as the commit ref. If no argument, let the skill auto-detect mode (feature branch vs single commit). Run the skill and produce the coverage report.
