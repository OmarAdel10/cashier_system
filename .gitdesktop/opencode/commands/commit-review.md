---
description: Review a commit or feature branch
---

Load the `commit-review` skill. If an argument is provided, pass it as the commit ref. If no argument, let the skill auto-detect mode (feature branch vs single commit). Run the skill and produce the review report.
